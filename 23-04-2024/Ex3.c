#include <stdio.h>
#include <stdlib.h>

int main()
{
    // int tamanho = 0;
    // printf("Qual vai ser o tamanho da palavra?\n");
    // scanf("%d", &tamanho);
    // char * string = malloc(tamanho * sizeof(char));
    // printf("%d\n", sizeof(string));
    // printf("Digite uma palavra: ");
    // scanf("%s", string);
    // printf("%s", string);
    // return 0;

    long double teste = 0;

    for(long double i = 0;;i+=1000000000000){
        printf("%d\n", i);
    }
}